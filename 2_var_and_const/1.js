'use strict';

//------------------------------------------------------------------------
// 1: 使用let定义变量  值不应该变的使用const(值不变对引用类型无用)
var max = 100;

var max = 200;

//可以真正的不可变 但也是顶级属性 嵌入式属性还是可变
const sam = Object.freeze({ first: 'Sam', age: 2 });
// 优先const 如果要变 再使用let